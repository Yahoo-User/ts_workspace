console.clear()
console.group("src/05-4/sample27.ts")

console.debug(`
*************************************************
* 05-4. Pure Function and Array                 *
*************************************************

3. Mutable And Immutable

(1) A variable with "const" or "readonly" type modifier always keeps the initial value. <--- ***

    - A variable like this called, "Immutable variable" that could *NOT* be changed.    <--- ***

    - Whereas, a variable with "let" or without "readonly" type modifier 
      could be always changed. So this variable called, "Mutable Variable".             <--- ***
`, '\n')



console.groupEnd()


